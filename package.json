{
  "name": "claudepod-v2",
  "version": "1.0.0",
  "description": "AI Development Platform with Claude Code",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}